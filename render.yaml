# Configuration Render pour GDELT Benin Assistant
# Ce fichier n'est pas nécessaire pour Render mais aide à la configuration

runtime = "python-3.10"
build_command = "pip install -r requirements.txt"
start_command = "uvicorn api:app --host 0.0.0.0 --port 8000"
